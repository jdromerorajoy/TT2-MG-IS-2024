import numpy as np

def predict_similarity(subgraph1, subgraph2):
    """Simula una red neuronal para calcular similitud entre subgrafos"""
    np.random.seed(42)  # Asegurar reproducibilidad
    return float(np.random.rand())  # Retorna un valor aleatorio entre 0 y 1